{
  "name": "sagar-ai-platform",
  "version": "1.0.0",
  "description": "SAGAR AI - Generative AI Super App Suite",
  "main": "start.js",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "node start.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "npm --prefix server start & npm --prefix client run dev",
    "test:pipeline": "npm --prefix server test"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  }
}
